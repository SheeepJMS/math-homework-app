# 本地运行与测试

## 1. 环境准备

- 已安装 **Python 3.8+**
- 在项目根目录打开终端（PowerShell 或 CMD）

## 2. 安装依赖

```powershell
# 进入项目目录（如未进入）
cd "c:\Users\Mush Zeng\OneDrive\桌面\MATH!!\Math coding cursor\Learning Coding beginner"

# 建议使用虚拟环境（可选）
python -m venv venv
.\venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt
```

若 `requirements.txt` 编码异常导致安装失败，可先安装核心依赖：

```powershell
pip install Flask Flask-Login Flask-SQLAlchemy Flask-WTF Flask-Migrate Werkzeug pandas openpyxl
```

## 3. 数据库（本地默认 SQLite）

- **不设置** `DATABASE_URL` 时，会自动使用：`instance/quiz.db`
- 首次运行前执行迁移，创建/更新表（含诊断模块的 `diag_*` 表）：

```powershell
# 设置 Flask 入口（PowerShell）
$env:FLASK_APP = "app.py"
flask db upgrade
```

若提示需要先 stamp 到当前版本：

```powershell
flask db stamp head
flask db upgrade
```

## 4. 启动服务

```powershell
python app.py
```

或：

```powershell
$env:FLASK_APP = "app.py"
flask run --host=0.0.0.0 --port=5000
```

浏览器访问：**http://127.0.0.1:5000**

## 5. 测试诊断模块

| 步骤 | 操作 |
|------|------|
| 1 | 访问 http://127.0.0.1:5000 用主系统账号登录（如 admin / admin123） |
| 2 | 点击顶部「题目管理系统」进入后台，再点 **诊断管理** |
| 3 | 在诊断管理里点 **导入 CSV**，上传项目里的 `diagnostic_sample.csv` |
| 4 | 在试卷列表中打开刚导入的试卷，点击 **发布** |
| 5 | 新开无痕/隐私窗口访问 http://127.0.0.1:5000/diagnostic |
| 6 | 注册一个诊断账号 → 登录 → 选择「诊断卷A」→ 开始答题 → 交卷 → 查看报告与练习包 |

## 6. 常见问题

- **端口被占用**：在 `app.py` 最后一行把 `port=5000` 改成其他端口（如 5001），或执行 `flask run --port=5001`
- **迁移报错**：先执行 `flask db current` 看当前版本，再 `flask db upgrade`；若数据库是新建的，可先 `flask db stamp b4fefe386c31` 再 `flask db upgrade`
- **pandas/numpy 报错**：多为版本不兼容，可尝试 `pip install --upgrade numpy pandas`
